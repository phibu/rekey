# PassReset — Milestone v1.2.3 Hotfix (ARCHIVED)

**Status:** ✅ Shipped 2026-04-14 · tag `v1.2.3`
**Phases:** 1 (Phase 01)
**Plans:** 3 / 3 complete
**Requirements delivered:** BUG-001, BUG-002, BUG-003 (3/3)
**Baseline:** v1.2.2

## Scope

Bugs-only hotfix — three P1 fixes delivered as an atomic release with no feature changes.

## Phase 01 — v1.2.3 Hotfix

**Goal:** Release v1.2.3 with all three P1 bugs fixed, no regressions to existing deployments.

**Plans:**
- ✅ 01-01 — BUG-001 SMTP internal-CA trust via thumbprint allowlist
- ✅ 01-02 — BUG-002 Map E_ACCESSDENIED to `ApiErrorCode.PasswordTooRecentlyChanged`
- ✅ 01-03 — BUG-003 Preserve IIS AppPool identity on upgrade

**Success criteria — all met:**
1. Operators can configure SMTP to trust an internal-CA-issued relay cert via an explicit `SmtpSettings.TrustedCertificateThumbprints` allowlist (SHA-1 or SHA-256). No silent bypass. Documented in `docs/appsettings-Production.md`.
2. A user who retries a password change within the domain `minPwdAge` window sees a dedicated "password changed too recently" message (German + English), backed by `ApiErrorCode.PasswordTooRecentlyChanged = 19` and a matching SIEM event.
3. `Install-PassReset.ps1` on upgrade preserves the existing IIS AppPool identity (no reset to `ApplicationPoolIdentity`).
4. v1.2.3 tag produced a released zip via `release.yml` with `CHANGELOG.md`, `UPGRADING.md`, and `docs/appsettings-Production.md` updated.

## Key Decisions

- Trusted-cert allowlist by thumbprint, not by CA — gives operators explicit control and avoids trusting an entire CA chain.
- E_ACCESSDENIED is mapped to `PasswordTooRecentlyChanged` only when the failure exception matches the expected pattern; other `0x80070005` failures still surface as generic errors (caught separately in backlog 999.1 for deeper diagnosis).
- Installer AppPool preservation implemented by reading current identity before uninstall and re-applying after install.

## Shipped Artifacts

- **Tag:** `v1.2.3`
- **Release asset:** `PassReset-v1.2.3.zip`
- **Date:** 2026-04-14

## Links

- Phase directory: `.planning/milestones/v1.2.3-phases/01-v1-2-3-hotfix/`
- PR: [#15](https://github.com/phibu/AD-Passreset-Portal/pull/15) — merged 2026-04-14
