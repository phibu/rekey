---
phase: 03-v1-3-ux-features
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/PassReset.Web/Models/ClientSettings.cs
  - src/PassReset.Web/Program.cs
  - src/PassReset.Web/appsettings.Production.template.json
  - src/PassReset.Web/appsettings.json
  - deploy/Install-PassReset.ps1
  - src/PassReset.Web/ClientApp/src/types/settings.ts
  - src/PassReset.Web/ClientApp/src/hooks/useSettings.ts
  - src/PassReset.Web/ClientApp/src/components/BrandHeader.tsx
  - src/PassReset.Web/ClientApp/src/App.tsx
  - docs/appsettings-Production.md
autonomous: true
requirements: [FEAT-001]
must_haves:
  truths:
    - "Operators can set CompanyName, PortalName, HelpdeskUrl, HelpdeskEmail, UsageText, LogoFileName, FaviconFileName, AssetRoot via appsettings.Production.json ClientSettings.Branding"
    - "Logo and favicon assets served from /brand/* via PhysicalFileProvider rooted at C:\\ProgramData\\PassReset\\brand\\ by default"
    - "Install-PassReset.ps1 creates the brand directory on fresh install and never overwrites/removes it on upgrade"
    - "Omitting the Branding block preserves the v1.2.3 default look (LockPersonIcon + 'PassReset' + current theme)"
    - "Favicon <link rel=icon> href updates at runtime when Branding.FaviconFileName is set"
    - "Helpdesk block renders URL as target=_blank rel=noopener link, email as mailto: link; block hidden if both absent"
  artifacts:
    - path: "src/PassReset.Web/Models/ClientSettings.cs"
      provides: "BrandingSettings nested class on ClientSettings"
      contains: "class BrandingSettings"
    - path: "src/PassReset.Web/ClientApp/src/components/BrandHeader.tsx"
      provides: "Branded header component with logo/icon fallback"
      exports: ["default"]
    - path: "src/PassReset.Web/Program.cs"
      provides: "PhysicalFileProvider /brand route"
      contains: "RequestPath = \"/brand\""
    - path: "deploy/Install-PassReset.ps1"
      provides: "Brand dir provisioning, upgrade-safe"
      contains: "ProgramData\\PassReset\\brand"
  key_links:
    - from: "src/PassReset.Web/Program.cs"
      to: "C:\\ProgramData\\PassReset\\brand"
      via: "PhysicalFileProvider + StaticFileOptions"
      pattern: "PhysicalFileProvider.*brand"
    - from: "src/PassReset.Web/ClientApp/src/App.tsx"
      to: "BrandHeader"
      via: "component replacement of hard-coded header"
      pattern: "import BrandHeader"
---

<objective>
Implement FEAT-001: operator branding — backend config shape, static-file wiring, installer provisioning, and frontend BrandHeader + favicon injection.

Purpose: Allow operators to customize company name, portal name, helpdesk URL/email, usage text, logo, and favicon via appsettings.Production.json without rebuilding. Defaults preserve v1.2.3 look.

Output: BrandingSettings class, PhysicalFileProvider mapping, brand dir created by installer, BrandHeader component, favicon runtime injection, type extensions.
</objective>

<execution_context>
@C:/Users/Phibu/Claude-Projekte/AD-Passreset-Portal/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Phibu/Claude-Projekte/AD-Passreset-Portal/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-v1-3-ux-features/03-CONTEXT.md
@.planning/phases/03-v1-3-ux-features/03-RESEARCH.md
@.planning/phases/03-v1-3-ux-features/03-UI-SPEC.md
@src/PassReset.Web/Models/ClientSettings.cs
@src/PassReset.Web/Program.cs
@src/PassReset.Web/ClientApp/src/App.tsx
@src/PassReset.Web/ClientApp/src/hooks/useSettings.ts
@src/PassReset.Web/ClientApp/src/types/settings.ts
@deploy/Install-PassReset.ps1

<interfaces>
From CONTEXT.md (locked decisions) — BrandingSettings shape:
```csharp
public sealed class BrandingSettings
{
    public string? CompanyName { get; set; }
    public string? PortalName { get; set; }
    public string? HelpdeskUrl { get; set; }
    public string? HelpdeskEmail { get; set; }
    public string? UsageText { get; set; }
    public string? LogoFileName { get; set; }
    public string? FaviconFileName { get; set; }
    public string? AssetRoot { get; set; }
}
// Added as: public BrandingSettings? Branding { get; set; } on ClientSettings
```

From RESEARCH.md Pattern 2 — PhysicalFileProvider wiring in Program.cs:
```csharp
var brandRoot = clientSettings.Branding?.AssetRoot
    ?? Path.Combine(Environment.GetFolderPath(Environment.SpecialFolder.CommonApplicationData),
                    "PassReset", "brand");
Directory.CreateDirectory(brandRoot);
app.UseStaticFiles(new StaticFileOptions
{
    FileProvider = new PhysicalFileProvider(brandRoot),
    RequestPath = "/brand",
    ServeUnknownFileTypes = false
});
```

TypeScript Branding type (mirrors C#):
```ts
export interface BrandingSettings {
  companyName?: string;
  portalName?: string;
  helpdeskUrl?: string;
  helpdeskEmail?: string;
  usageText?: string;
  logoFileName?: string;
  faviconFileName?: string;
  assetRoot?: string;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add BrandingSettings to ClientSettings + wire PhysicalFileProvider + installer brand dir + template/default configs</name>
  <files>src/PassReset.Web/Models/ClientSettings.cs, src/PassReset.Web/Program.cs, src/PassReset.Web/appsettings.Production.template.json, src/PassReset.Web/appsettings.json, deploy/Install-PassReset.ps1, docs/appsettings-Production.md</files>
  <read_first>
    - src/PassReset.Web/Models/ClientSettings.cs (full file — understand current structure for nested class placement)
    - src/PassReset.Web/Program.cs (full file — find existing app.UseStaticFiles line and routing order)
    - src/PassReset.Web/appsettings.Production.template.json (current shape)
    - deploy/Install-PassReset.ps1 (identify install vs upgrade branches)
    - .planning/phases/03-v1-3-ux-features/03-CONTEXT.md (locked branding decisions)
    - .planning/phases/03-v1-3-ux-features/03-RESEARCH.md (Pattern 2 — PhysicalFileProvider; §FEAT-001 installer notes)
  </read_first>
  <action>
    1. In `src/PassReset.Web/Models/ClientSettings.cs`, add a `public sealed class BrandingSettings` with nullable string properties: `CompanyName`, `PortalName`, `HelpdeskUrl`, `HelpdeskEmail`, `UsageText`, `LogoFileName`, `FaviconFileName`, `AssetRoot`. Add `public BrandingSettings? Branding { get; set; }` property on `ClientSettings`. All fields serialize as JSON (no `[JsonIgnore]`) — Branding contains no secrets per CONTEXT.md cross-cutting.
    2. In `src/PassReset.Web/Program.cs`, after the existing `app.UseStaticFiles()` call (default wwwroot), and BEFORE `app.UseRouting()`, add the PhysicalFileProvider block from `<interfaces>`. Resolve `clientSettings` from the already-configured `IOptions<ClientSettings>` value (use `app.Services.GetRequiredService<IOptions<ClientSettings>>().Value` if not already available). Ensure `using Microsoft.Extensions.FileProviders;` is added.
    3. In `src/PassReset.Web/appsettings.Production.template.json` and `src/PassReset.Web/appsettings.json`, add a commented-out or empty `"Branding": { "CompanyName": "", "PortalName": "PassReset", "HelpdeskUrl": "", "HelpdeskEmail": "", "UsageText": "", "LogoFileName": "", "FaviconFileName": "", "AssetRoot": null }` block under `ClientSettings`. Note in comments: "Omit entire Branding block to keep v1.2.3 default look."
    4. In `deploy/Install-PassReset.ps1`, add a provisioning step that runs on BOTH fresh install and upgrade paths: if `$env:ProgramData\PassReset\brand` does not exist, create it with `New-Item -ItemType Directory -Force`. On upgrade, do NOT remove or overwrite the directory or any files inside it. Add a comment: `# BRAND DIR — upgrade-safe, never remove on upgrade per FEAT-001 / D-Branding`.
    5. In `docs/appsettings-Production.md`, add a new "### Branding (FEAT-001)" subsection documenting all 8 Branding keys, the default `C:\ProgramData\PassReset\brand\` asset path, and that omitting the block preserves the current look.
  </action>
  <verify>
    <automated>dotnet build src/PassReset.sln --configuration Release</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "class BrandingSettings" src/PassReset.Web/Models/ClientSettings.cs` returns one match
    - `grep -n "public BrandingSettings? Branding" src/PassReset.Web/Models/ClientSettings.cs` returns one match
    - `grep -n "RequestPath = \"/brand\"" src/PassReset.Web/Program.cs` returns one match
    - `grep -n "PhysicalFileProvider" src/PassReset.Web/Program.cs` returns at least one match
    - `grep -n "ProgramData.*PassReset.*brand" deploy/Install-PassReset.ps1` returns at least one match
    - `grep -n "Branding" src/PassReset.Web/appsettings.Production.template.json` returns at least one match
    - `docs/appsettings-Production.md` contains the string "Branding (FEAT-001)" or "### Branding"
    - `dotnet build src/PassReset.sln --configuration Release` exits 0
  </acceptance_criteria>
  <done>Backend config shape, static-file wiring, installer provisioning, and docs complete. Build green.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Frontend BrandingSettings type + useSettings extension + BrandHeader component + App.tsx wiring + runtime favicon injection</name>
  <files>src/PassReset.Web/ClientApp/src/types/settings.ts, src/PassReset.Web/ClientApp/src/hooks/useSettings.ts, src/PassReset.Web/ClientApp/src/components/BrandHeader.tsx, src/PassReset.Web/ClientApp/src/App.tsx</files>
  <read_first>
    - src/PassReset.Web/ClientApp/src/types/settings.ts (current ClientSettings interface)
    - src/PassReset.Web/ClientApp/src/hooks/useSettings.ts (current fetch + return shape)
    - src/PassReset.Web/ClientApp/src/App.tsx (current hard-coded header block — lines around LockPersonIcon + portal title)
    - .planning/phases/03-v1-3-ux-features/03-UI-SPEC.md (Component Inventory §1 BrandHeader + Typography + Color sections)
    - .planning/phases/03-v1-3-ux-features/03-CONTEXT.md (FEAT-001 locked decisions — helpdesk rendering rules)
  </read_first>
  <action>
    1. In `src/PassReset.Web/ClientApp/src/types/settings.ts`, add the `BrandingSettings` interface from `<interfaces>` above (camelCase property names since C# JSON serializer uses camelCase by default — verify by inspecting existing types in the file). Add `branding?: BrandingSettings;` to the `ClientSettings` interface.
    2. In `src/PassReset.Web/ClientApp/src/hooks/useSettings.ts`, no logic change needed beyond the type surfacing (Branding arrives automatically via the existing `/api/password` GET). Confirm the returned settings expose `.branding`.
    3. Create `src/PassReset.Web/ClientApp/src/components/BrandHeader.tsx` as a default-export functional component accepting `{ branding }: { branding?: BrandingSettings }`. Render per UI-SPEC §1 BrandHeader:
       - If `branding?.logoFileName` set: `<img src={`/brand/${branding.logoFileName}`} alt={branding?.portalName ?? 'PassReset'}>` with MUI sx `{ maxHeight: 48, maxWidth: 200, objectFit: 'contain' }`, and `onError` handler that swaps state to fallback icon+text.
       - Else: `<LockPersonIcon sx={{ fontSize: 28, color: 'primary.main' }} />` + `<Typography variant="h6" fontWeight={600}>{branding?.portalName ?? 'PassReset'}</Typography>`.
       - Layout via MUI `<Stack direction="row" alignItems="center" spacing={1}>`.
    4. In `src/PassReset.Web/ClientApp/src/App.tsx`:
       - Import BrandHeader. Replace the existing hard-coded header block (LockPersonIcon + title Typography) with `<BrandHeader branding={settings?.branding} />`.
       - Add a `useEffect` that, when `settings?.branding?.faviconFileName` changes, updates `document.querySelector('link[rel="icon"]')?.setAttribute('href', '/brand/' + settings.branding.faviconFileName)`. If no link element exists, create one: `const l = document.createElement('link'); l.rel = 'icon'; l.href = '/brand/' + settings.branding.faviconFileName; document.head.appendChild(l);`. Guard: only run when `faviconFileName` is truthy.
       - Render helpdesk block per CONTEXT.md D-FEAT-001 rule: if `branding?.helpdeskUrl` or `branding?.helpdeskEmail` is set, render a small footer/aside block with `<a href={helpdeskUrl} target="_blank" rel="noopener">` (if present) and `<a href={`mailto:${helpdeskEmail}`}>` (if present). If both absent, render nothing for this block.
       - If `branding?.usageText` set, render above form as `<Typography variant="body2" color="text.secondary" sx={{ mb: 2 }}>{branding.usageText}</Typography>` replacing prior `form.helpText` fallback only when usageText is set.
  </action>
  <verify>
    <automated>cd src/PassReset.Web/ClientApp && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "export default function BrandHeader" src/PassReset.Web/ClientApp/src/components/BrandHeader.tsx` returns one match
    - `grep -n "BrandingSettings" src/PassReset.Web/ClientApp/src/types/settings.ts` returns at least one match
    - `grep -n "branding" src/PassReset.Web/ClientApp/src/types/settings.ts` returns at least one match (property on ClientSettings)
    - `grep -n "import BrandHeader" src/PassReset.Web/ClientApp/src/App.tsx` returns one match
    - `grep -n "faviconFileName" src/PassReset.Web/ClientApp/src/App.tsx` returns at least one match
    - `grep -n "/brand/" src/PassReset.Web/ClientApp/src/App.tsx` returns at least one match
    - `cd src/PassReset.Web/ClientApp && npm run build` exits 0 (tsc type-check + Vite build pass)
  </acceptance_criteria>
  <done>BrandHeader rendering with fallback, favicon injection working, helpdesk block, and usage text wired. Build + type-check clean.</done>
</task>

</tasks>

<verification>
- Build: `dotnet build src/PassReset.sln --configuration Release` passes.
- Frontend build: `cd src/PassReset.Web/ClientApp && npm run build` passes.
- Manual: With empty Branding config → UI identical to v1.2.3 (LockPersonIcon + "PassReset").
- Manual: With logo + favicon files dropped in `C:\ProgramData\PassReset\brand\` and config referencing them → rendered via `/brand/*` URLs.
- Install-PassReset.ps1 upgrade path does NOT touch existing `C:\ProgramData\PassReset\brand\` contents.
</verification>

<success_criteria>
- [ ] FEAT-001 Success Criterion #1 (ROADMAP): Operator can set all 8 branding fields via appsettings.Production.json; served from upgrade-safe path; defaults preserve current look
- [ ] ClientSettings serialization round-trips Branding without secrets exposure
- [ ] No breaking config changes (pre-v1.3 appsettings continue to work — omitting Branding produces v1.2.3 behavior)
</success_criteria>

<output>
After completion, create `.planning/phases/03-v1-3-ux-features/03-01-SUMMARY.md` describing files touched, decisions implemented (D-FEAT-001), and any follow-ups.
</output>
