---
phase: 02-v1-3-test-foundation
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - src/PassReset.Tests/PassReset.Tests.csproj
  - src/PassReset.Tests/GlobalUsings.cs
  - src/PassReset.Tests/SmokeTests.cs
  - src/PassReset.sln
  - src/PassReset.Web/Program.cs
autonomous: true
requirements: [QA-001]
must_haves:
  truths:
    - "A test project exists at src/PassReset.Tests and is wired into the solution"
    - "dotnet test src/PassReset.sln runs and executes at least one xUnit v3 test (smoke)"
    - "WebApplicationFactory<Program> can reference Program via public partial declaration"
  artifacts:
    - path: "src/PassReset.Tests/PassReset.Tests.csproj"
      provides: "xUnit v3 test project with TFM net10.0-windows, coverlet.msbuild, NSubstitute, Mvc.Testing"
      contains: "xunit.v3"
    - path: "src/PassReset.Tests/SmokeTests.cs"
      provides: "Trivial smoke test proving the runner executes"
    - path: "src/PassReset.Web/Program.cs"
      provides: "public partial class Program { } marker for WebApplicationFactory<Program>"
      contains: "public partial class Program"
  key_links:
    - from: "src/PassReset.sln"
      to: "src/PassReset.Tests/PassReset.Tests.csproj"
      via: "sln Project() entry"
      pattern: "PassReset.Tests"
---

<objective>
Scaffold the xUnit v3 backend test project so downstream test-authoring plans have a working target. Remove stale bin/obj, add the project to the solution, append the `public partial class Program {}` marker needed for WebApplicationFactory<Program>, and prove `dotnet test` runs end-to-end with a single passing smoke test.

Purpose: Establishes Success Criterion #1 (dotnet test runs on a clean checkout) foundation; unblocks 02-02.
Output: Working PassReset.Tests project + Program.cs seam.
</objective>

<execution_context>
@C:/Users/Phibu/Claude-Projekte/AD-Passreset-Portal/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Phibu/Claude-Projekte/AD-Passreset-Portal/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-v1-3-test-foundation/02-CONTEXT.md
@.planning/phases/02-v1-3-test-foundation/02-RESEARCH.md
@src/PassReset.sln
@src/PassReset.Web/Program.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete stale test artifacts and scaffold csproj</name>
  <files>src/PassReset.Tests/PassReset.Tests.csproj, src/PassReset.Tests/GlobalUsings.cs, src/PassReset.Tests/SmokeTests.cs</files>
  <read_first>
    - src/PassReset.Tests/ (verify only bin/ and obj/ present, no .cs or .csproj)
    - .planning/phases/02-v1-3-test-foundation/02-RESEARCH.md (pattern section for csproj template)
    - src/PassReset.Common/PassReset.Common.csproj (reference style for PackageReference formatting)
  </read_first>
  <action>
    1. Remove stale artifacts: `rm -rf src/PassReset.Tests/bin src/PassReset.Tests/obj`
    2. Create `src/PassReset.Tests/PassReset.Tests.csproj` with this exact content:

    ```xml
    <Project Sdk="Microsoft.NET.Sdk">
      <PropertyGroup>
        <TargetFramework>net10.0-windows</TargetFramework>
        <Nullable>enable</Nullable>
        <ImplicitUsings>enable</ImplicitUsings>
        <IsPackable>false</IsPackable>
        <IsTestProject>true</IsTestProject>
        <OutputType>Exe</OutputType>
      </PropertyGroup>
      <ItemGroup>
        <PackageReference Include="Microsoft.NET.Test.Sdk" Version="17.11.*" />
        <PackageReference Include="xunit.v3" Version="3.2.2" />
        <PackageReference Include="xunit.runner.visualstudio" Version="3.*">
          <IncludeAssets>runtime; build; native; contentfiles; analyzers; buildtransitive</IncludeAssets>
          <PrivateAssets>all</PrivateAssets>
        </PackageReference>
        <PackageReference Include="coverlet.msbuild" Version="8.0.1">
          <IncludeAssets>runtime; build; native; contentfiles; analyzers; buildtransitive</IncludeAssets>
          <PrivateAssets>all</PrivateAssets>
        </PackageReference>
        <PackageReference Include="NSubstitute" Version="5.3.0" />
        <PackageReference Include="Microsoft.AspNetCore.Mvc.Testing" Version="10.0.*" />
      </ItemGroup>
      <ItemGroup>
        <ProjectReference Include="..\PassReset.Common\PassReset.Common.csproj" />
        <ProjectReference Include="..\PassReset.PasswordProvider\PassReset.PasswordProvider.csproj" />
        <ProjectReference Include="..\PassReset.Web\PassReset.Web.csproj" />
      </ItemGroup>
    </Project>
    ```

    3. Create `src/PassReset.Tests/GlobalUsings.cs`:
    ```csharp
    global using Xunit;
    global using NSubstitute;
    ```

    4. Create `src/PassReset.Tests/SmokeTests.cs`:
    ```csharp
    namespace PassReset.Tests;

    public class SmokeTests
    {
        [Fact]
        public void TestRunner_Works()
        {
            Assert.True(true);
        }
    }
    ```

    IMPORTANT: If any of the exact version lines fail NuGet restore because a package is unavailable, run `dotnet add package <name>` without `--version` to let NuGet resolve, then pin the resolved version in the csproj. Do NOT downgrade xunit to v2. Do NOT switch coverlet.msbuild to coverlet.collector (research override of CONTEXT D6).
  </action>
  <verify>
    <automated>dotnet restore src/PassReset.Tests/PassReset.Tests.csproj</automated>
  </verify>
  <done>
    csproj, GlobalUsings.cs, SmokeTests.cs exist at the specified paths. `dotnet restore` on the tests csproj succeeds with no errors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add test project to solution and add Program.cs partial marker</name>
  <files>src/PassReset.sln, src/PassReset.Web/Program.cs</files>
  <read_first>
    - src/PassReset.sln (understand existing project entries)
    - src/PassReset.Web/Program.cs (identify end of file)
  </read_first>
  <action>
    1. Run: `dotnet sln src/PassReset.sln add src/PassReset.Tests/PassReset.Tests.csproj`
    2. Append this block to the END of `src/PassReset.Web/Program.cs` (after the last closing brace or top-level statements terminator):
    ```csharp

    // Marker type to allow WebApplicationFactory<Program> in test projects.
    // Top-level programs generate an internal Program class; this makes it public.
    public partial class Program { }
    ```
    If `Program.cs` already ends with a non-top-level namespace/class, append the partial inside the appropriate namespace. The marker MUST be `public partial class Program` at the same namespace level as the implicit Program class (top-level statements → global namespace; no namespace wrapper needed).

    3. Verify solution + build compiles: `dotnet build src/PassReset.sln -c Release`
  </action>
  <verify>
    <automated>dotnet build src/PassReset.sln --configuration Release</automated>
  </verify>
  <done>
    `dotnet sln list` shows PassReset.Tests. `dotnet build src/PassReset.sln -c Release` succeeds with 0 errors. `grep "public partial class Program" src/PassReset.Web/Program.cs` returns a match.
  </done>
</task>

<task type="auto">
  <name>Task 3: Prove test runner executes smoke test end-to-end</name>
  <files>(none new — verification only)</files>
  <read_first>
    - src/PassReset.Tests/SmokeTests.cs (confirm present)
  </read_first>
  <action>
    Run `dotnet test src/PassReset.sln --configuration Release --no-build` from the repo root. Output MUST contain a passing line for `PassReset.Tests.SmokeTests.TestRunner_Works`. If xunit.v3 requires the EXE to be launched differently and `dotnet test` fails with "no tests found", add `<UseMicrosoftTestingPlatformRunner>false</UseMicrosoftTestingPlatformRunner>` to the csproj PropertyGroup to force VSTest host (per research Pitfall — MTP+coverlet not yet compatible).
  </action>
  <verify>
    <automated>dotnet test src/PassReset.sln --configuration Release --no-build --filter "FullyQualifiedName=PassReset.Tests.SmokeTests.TestRunner_Works"</automated>
  </verify>
  <done>
    `dotnet test` exits 0 and reports "Passed: 1" (or higher) including TestRunner_Works. No "no tests discovered" warning.
  </done>
</task>

</tasks>

<verification>
- `dotnet build src/PassReset.sln -c Release` → 0 errors
- `dotnet test src/PassReset.sln -c Release --no-build` → exit 0, ≥1 passing test
- `grep -c "public partial class Program" src/PassReset.Web/Program.cs` → 1
- `grep -c "PassReset.Tests" src/PassReset.sln` → ≥2 (Project entry + configuration)
</verification>

<success_criteria>
Scaffolding complete when all verification commands pass and the next plan (02-02) can add test classes without further csproj/sln edits.
</success_criteria>

<output>
After completion, create `.planning/phases/02-v1-3-test-foundation/02-01-SUMMARY.md` capturing installed package versions, any csproj deviations, and any issues hit.
</output>
