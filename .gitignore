# ── .NET build output ────────────────────────────────────────────────────────
bin/
obj/
*.user
*.suo
.vs/
.vscode/settings.json
*.ncrunch*

# ── Frontend build output (Vite → wwwroot) ───────────────────────────────────
# Generated by `npm run build` — never commit. Run Publish-ReKey.ps1 instead.
src/ReKey.Web/wwwroot/

# ── Node ─────────────────────────────────────────────────────────────────────
node_modules/

# ── Deploy publish output and release zips ───────────────────────────────────
deploy/publish/
deploy/ReKey-*.zip

# ── Secrets — never commit ───────────────────────────────────────────────────
appsettings.Production.json
appsettings.*.local.json
secrets.json
*.pfx
*.p12

# ── Local tooling / IDE metadata ─────────────────────────────────────────────
.claude/
.bg-shell/
.gsd/

# ── Original upstream reference (kept locally for reference, not part of V2) ─
passcore/

# ── OS ───────────────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
*.swp
